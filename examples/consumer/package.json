{
  "name": "quantus-consumer-example",
  "private": true,
  "type": "module",
  "description": "Uses @quantus-network/wasm and @quantus-network/wormhole the way an application would: ML-DSA-87 and ML-DSA-65 signing, wormhole deposit and exit against a node.",
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@polkadot/api": "^16.5.6",
    "@polkadot/util-crypto": "^14.0.3",
    "@quantus-network/wasm": "file:../..",
    "@quantus-network/wormhole": "file:../../wormhole/native"
  },
  "engines": {
    "node": ">=18"
  }
}
